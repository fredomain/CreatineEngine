#include "Fondo1.h"

Fondo1::Fondo1(CE::Scene& scene) : scene(scene) {
	/*if ()
	 static std::unique_ptr<CE::Texture> texture = std::make_unique<CE::Texture>(
		CE::SceneManager::getWindowRenderer(),
		"Content/background.png", scene.getAssetLoaderManager());*/
}
